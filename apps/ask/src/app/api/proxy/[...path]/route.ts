// Same-origin gateway proxy — shared implementation (Set-Cookie-forwarding,
// httpOnly-cookie aware). Reads API_GATEWAY_URL from env.
export { GET, POST, PUT, DELETE, PATCH } from "@atpost/api-client/proxy"
