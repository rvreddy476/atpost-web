{
  "name": "@momentum/brand",
  "version": "0.1.0",
  "private": true,
  "description": "Product identity as constants — the name, the wordmark, zone titles, support text, and the frozen storage/event keys a rename must not touch. One place to edit when the product is renamed.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "sideEffects": false,
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "lint": "eslint src --ext .ts --max-warnings=0"
  },
  "devDependencies": {
    "@atpost/config": "workspace:*",
    "typescript": "^5.8.2",
    "vitest": "2.1.9"
  },
  "files": [
    "src"
  ]
}
