{
  "name": "@momentum/notifications",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "The bell and the inbox behind it. Unread count, the first page, the next page, mark read and mark all read, against /v1/notifications on the gateway. The second package allowed to touch @atpost/api-client, and for the same reason as the chrome: the inbox is the viewer's, identical in every zone.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "sideEffects": false,
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "lint": "eslint src --ext .ts,.tsx --max-warnings=0"
  },
  "dependencies": {
    "@atpost/api-client": "workspace:*",
    "@momentum/content": "workspace:*"
  },
  "peerDependencies": {
    "lucide-react": ">=0.400.0",
    "react": ">=18",
    "react-dom": ">=18"
  },
  "devDependencies": {
    "@atpost/config": "workspace:*",
    "@types/react": "^19.2.2",
    "@types/react-dom": "^19.2.2",
    "lucide-react": "^0.460.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "typescript": "^5.8.2",
    "vitest": "2.1.9"
  },
  "files": [
    "src"
  ]
}
